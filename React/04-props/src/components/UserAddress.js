export function UserAddress({ street, city, country }) {
    return (
        <div>
            <p>Street: {street}</p>
            <p>City: {city}</p>
            <p>Country: {country}</p>
        </div>
    );
}